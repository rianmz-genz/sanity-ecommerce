import schemas from './schemas'
export const schemaTypes = [
  schemas.discount,
  schemas.order,
  schemas.orderLine,
  schemas.payment,
  schemas.product,
  schemas.productCategory,
  schemas.shippingAddress,
  schemas.user,
  schemas.cart,
  schemas.cartItem,
]
